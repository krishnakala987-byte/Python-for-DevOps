# F-Strings in Python

F-strings are used for dynamic string formatting.

Example:

```python
env = "prod"

print(f"Deploying to {env}")
```

Output:

```text
Deploying to prod
```

Used heavily in DevOps automation scripts.
